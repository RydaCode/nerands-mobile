import { FontAwesome, FontAwesome5, MaterialCommunityIcons } from '@expo/vector-icons';
import { useLocalSearchParams, useRouter } from 'expo-router';
import { useEffect, useState } from 'react';
import { FlatList, Image, Text, TouchableOpacity, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import Toast from 'react-native-toast-message';
import MainHeader from '../../../components/MainHeader';
import { COLORS } from '../../../constants/constants';
import useApi from '../../../hook/useApi';
import { USER_IMAGE_URI } from '../../../RequestMethods';
import LoadingIndicator from '../../LoadingIndicator';

const index = () => {
    const router = useRouter();
    const params = useLocalSearchParams();

    const [settings, setSettings] = useState(false);
    const [assignement, setAssignement] = useState(false);
    const [buyrides, setBuyRides] = useState(false);
    const [comletesettingaccount, comleteSettingAccount] = useState(false);

    const transporter_id = '20250224_1629007291740407340729_67bc822cb1de46.283611430da19cbb202460a3e5c9b65232566259';

    const {data:getpaidrides, isLoading:getpaidloading, error:getpaiderrors, get:getpaidrefetch} = useApi(`/trip/get_paid_trips/${transporter_id}`);
    const {data:gettransporter, isLoading:transporterloading, error:transportererrors, get:transporterrefetch} = useApi(`/deliveryman/transporter/${transporter_id}`);
    const {data:getalltrips, isLoading:alltripsloading, error:alltripserrors, get:alltripsrefetch} = useApi(`/trip/all_trips/`);

    const [selectedTrip, setSelectedTrip] = useState(null);
    const [isSuccess, setIsSuccess] = useState(false);

    // Initialize the useSend hook at the component level
    const { data:buytrips, resend:resendbuytrips, isLoading:buytripsloading, error:buytripserror } = useApi("/trip/purchase_trip/", selectedTrip);

    const {data: orderdata, isLoading: isLoadingOrder, error: errororder} = useApi(`/order/nutral_single_order/${gettransporter?.assign_order_id}/`);
    // When selectedTrip updates, resend API request
    useEffect(() => {
        if (selectedTrip !== null) {
            resendbuytrips();
        }
    }, [selectedTrip]);

    // Handles trip purchase
    const handleBuyTrip = (tripData) => {
        setSelectedTrip(tripData);
    };

    // Show toast when `send` updates (success)
    useEffect(() => {
        if (buytrips) {
            if (buytrips?.Response === 'Success') {
                setIsSuccess(true);
                showToast('success', '✅ Purchase Successful!', 'Your trips has been purchased.');
                setBuyRides(false);
            }
            else{
                showToast('error', '❌ Purchase Failure!', buytrips?.Response);
            }
        }
    }, [buytrips]);

    // Show toast when `error` updates (failure)
    useEffect(() => {
        if (buytripserror) {
            showToast('error', '❌ Purchase Failed', buytripserror.message || 'Something went wrong');
            setBuyRides(false);
        }
    }, [buytripserror]);
    
    const showToast = (type, text1, text2) => {
        Toast.show({
            type,
            text1,
            text2,
            position: 'bottom',
            visibilityTime: 4000,
            style: {
                backgroundColor: type === 'error' ? '#f8d7da' : '#d4edda',
                borderLeftColor: type === 'error' ? 'red' : '#32CD32',
                borderLeftWidth: 5,
                padding: 10,
            },
            text1Style: {
                fontSize: 14,
                color: type === 'error' ? 'red' : '#32CD32',
                fontFamily: 'maven-bold'
            },
            text2Style: {
                fontSize: 14,
                color: type === 'error' ? 'red' : '#32CD32',
            },
        });
    };

    // console.log(orderdata)

    return (
        <SafeAreaView className="flex-1 bg-white items-center relative">
            {/* Header */}
            <View className="px-4">
                <MainHeader fontFamily="maven-bold" header_name="Transporter" />
            </View>

            {/* Transporter Content */}
            {is_transporter === false ? (
                // If not registered
                <CreateTransporterAcc router={router}/>
            ) : (
                // If registered → show stats
                <>
                    {/* Modals */}
                    <CompleteAccountSetupModal transporter={gettransporter} />
                    <BuyTripsModal
                        visible={buyRides}
                        trips={getalltrips}
                        onClose={() => setBuyRides(false)}
                        transporter_id={user_id}
                    />
                    <SettingsModal visible={settings} onClose={() => setSettings(false)} />
                    <AssignmentModal
                        visible={assignment}
                        onClose={() => setAssignment(false)}
                    />

                    <FlatList
                        ListHeaderComponent={() => (
                            <View className="w-full mb-10">
                                <View className="mt-2 px-2 w-full flex-row justify-between items-center">
                                    <View
                                        className="rounded-full border-2 border-lavender"
                                        style={{ height: 70, width: 70 }}
                                    >
                                        <Image
                                            className="h-full w-full rounded-full"
                                            source={{
                                                uri: `${USER_IMAGE_URI}${profile_image}`,
                                            }}
                                        />
                                    </View>
                                    <View className="mx-2" style={{ width: '60%' }}>
                                        <Text
                                            className="text-lg"
                                            style={{ fontFamily: 'roboto-medium' }}
                                        >{first_name} {last_name}</Text>
                                        <Text
                                            className="text-sm text-slate"
                                            style={{ fontFamily: 'roboto-medium' }}
                                        >{phone_num}</Text>
                                    </View>
                                    <TouchableOpacity
                                        onPress={() => setSettings(true)}
                                        className="border-2 border-lavender rounded-full justify-center items-center"
                                        style={{ height: 47, width: 47 }}
                                    >
                                        <View className="w-full h-full bg-grey_bg rounded-full border-2 border-white items-center justify-center">
                                            <Text>⚙️</Text>
                                        </View>
                                    </TouchableOpacity>
                                </View>

                                {/* Trips Summary */}
                                <View className="h-full">
                                    <View className="w-full flex-row justify-between items-center px-4 mt-8">
                                        <View
                                            className="bg-grey_bg rounded-md justify-center items-center"
                                            style={{ width: '48%', height: 100 }}
                                        >
                                            <Text
                                                className="text-xl"
                                                style={{ fontFamily: 'maven-bold' }}
                                            >
                                                {getpaidrides?.trips || 0}
                                            </Text>
                                            <Text
                                                className="text-base"
                                                style={{ fontFamily: 'maven-medium' }}
                                            >
                                                Available Trips
                                            </Text>
                                        </View>
                                        <View
                                            className="bg-grey_bg rounded-md justify-center items-center"
                                            style={{ width: '48%', height: 100 }}
                                        >
                                            <Text
                                                className="text-xl"
                                                style={{ fontFamily: 'maven-bold' }}
                                            >5</Text>
                                            <Text
                                                className="text-base"
                                                style={{ fontFamily: 'maven-medium' }}
                                            >Completed Trips</Text>
                                        </View>
                                    </View>
                                </View>

                                {/* Buy Trips / Maps / Enable */}
                                <View className="w-full flex-row justify-between items-center px-4 mt-4">
                                    <TouchableOpacity
                                        className="bg-green2 rounded-md justify-center items-center"
                                        style={{ width: '32%', height: 70 }}
                                        onPress={() => setBuyRides(true)}
                                    >
                                        <Text>🚴</Text>
                                        <Text
                                            className="text-base text-white"
                                            style={{ fontFamily: 'maven-medium' }}
                                        >Buy Trips</Text>
                                    </TouchableOpacity>
                                    <TouchableOpacity
                                        className="bg-green2 rounded-md justify-center items-center"
                                        style={{ width: '32%', height: 70 }}
                                    >
                                        <Text className="text-xl">📍</Text>
                                        <Text
                                            className="text-base text-white"
                                            style={{ fontFamily: 'maven-medium' }}
                                        >Goto Maps</Text>
                                    </TouchableOpacity>
                                    <TouchableOpacity
                                        className="bg-red rounded-md justify-center items-center"
                                        style={{ width: '32%', height: 70 }}
                                    >
                                        <Text className="text-white">Enable</Text>
                                    </TouchableOpacity>
                                </View>
                            </View>
                        )}
                    />
                </>
            )}
            <Toast />
        </SafeAreaView>
    )
}

export default index
        
            <TouchableOpacity
                onPress={() => setOrderFullInfoModalVisible(true)}
                className='flex-row justify-between items-center'
            >
                <View className='flex-row justify-start items-center'>
                    <Image
                        source={{uri:`${PRODUCTS_IMAGE_URI}${order.product_image}`}}
                        style={{borderRadius: SIZES.radius}} className='h-[65px] w-[26%]'
                    />
                    <View className='w-[71.7%] flex-row ml-2 justify-between items-center'>
                        <View className='w-[90%]'>
                            <View className=''>
                                <Text className='text-md' style={{fontFamily: 'maven-bold'}}>{order.product_name}</Text>
                            </View>
                            <View className='flex-row justify-between items-center'>
                                <View>
                                    <Text className='text-lg text-primary' style={{fontFamily: 'maven-bold'}}>K{order.product_price}</Text>
                                </View>
                                <View className='flex-row items-center justify-start mr-4'>
                                    <Text className='text-slate text-sm' style={{fontFamily: 'maven-medium',}}>Qty:{order.quantity}</Text>
                                </View>
                            </View>
                            <View className='flex-row justify-between items-center'>
                                <View className='flex-row items-center justify-start mr-4'>
                                    <Text numberOfLines={1} className='text-grey text-sm' style={{fontFamily: 'maven-medium'}}>{order.desc}</Text>
                                </View>
                            </View>
                        </View>
                        <TouchableOpacity className='w-[8%] h-[50px] items-center justify-center'>
                            <FontAwesome name='times' size={19} color={COLORS.red} />
                        </TouchableOpacity>
                    </View>
                </View>
            </TouchableOpacity>